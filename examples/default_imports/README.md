# Default Import Files

These files are ready to be imported in PromptOps with baseline configurations.

## Providers
- `promptops_default_providers.xlsx`
- `promptops_default_providers.json`

## Endpoints
- `promptops_default_endpoints.xlsx`
- `promptops_default_endpoints.json`

## Test Suites + Prompts
- `promptops_default_test_suites.xlsx`
- `promptops_default_test_suites.json`

Notes:
- Endpoint `api_token` values are intentionally blank for security.
- Update keys/tokens after importing endpoints.
- Test suite files include suite metadata and prompts in the same structure.
